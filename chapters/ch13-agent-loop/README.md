# Chapter 13 Exercise: Minimal Agent Loop

Build a bounded executor with typed model steps, a tool registry, tool-result
history, and max-step protection.

Interview signal: the candidate can convert an agent concept into deterministic
control flow with clear failure modes.

Run:

```bash
python3 -B -m pytest -p no:cacheprovider chapters/ch13-agent-loop/tests
```
