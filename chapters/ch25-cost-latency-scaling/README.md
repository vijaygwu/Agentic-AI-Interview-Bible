# Chapter 25 Exercise: Task Budgets

Enforce model-call and token budgets for one agent task.

Interview signal: the candidate can treat cost and latency as product behavior,
not only as infrastructure concerns.

Run:

```bash
python3 -B -m pytest -p no:cacheprovider chapters/ch25-cost-latency-scaling/tests
```
