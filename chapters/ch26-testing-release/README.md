# Chapter 26 Exercise: Release Gate

Use an eval report to decide whether a prompt or model release can continue.

Interview signal: the candidate treats prompts, models, evals, policies, and
tool schemas as release artifacts.

Run:

```bash
python3 -B -m pytest -p no:cacheprovider chapters/ch26-testing-release/tests
```
